language: python
python:
  - "3.6"
install:
  - pip install .
  - pip install -r requirements.txt
  - python -m nltk.downloader stopwords
script: pytest
notifications:
  email:
    - 3innielee@gmail.com
